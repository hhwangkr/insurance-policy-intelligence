[project]
name = "insurance-ai-ingestion"
version = "0.1.0"
description = "Document ingestion and normalization (reserved)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
