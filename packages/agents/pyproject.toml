[project]
name = "insurance-ai-agents"
version = "0.1.0"
description = "LangGraph agent workflows (reserved)."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
